import { checkPolarity } from "../client/js/polarityCheck";

describe('Test the polarity checker', () => {
    test('Test checkPolarity() function', () => {
        expect(checkPolarity).toBeDefined();
    })

});